import { NextResponse } from "next/server";import { z } from "zod";import { createSession,COOKIE_NAME } from "@/lib/auth";
const schema=z.object({email:z.string().email(),password:z.string().min(1)});
export async function POST(req:Request){const parsed=schema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:"Invalid credentials"},{status:401});const email=process.env.ADMIN_EMAIL||"admin@yakafinity.com";const password=process.env.ADMIN_PASSWORD||"change-this-password";if(parsed.data.email!==email||parsed.data.password!==password)return NextResponse.json({error:"Invalid credentials"},{status:401});const res=NextResponse.json({ok:true});res.cookies.set(COOKIE_NAME,await createSession(email),{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",path:"/",maxAge:60*60*8});return res;}
