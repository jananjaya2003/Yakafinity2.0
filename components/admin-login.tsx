"use client";import { FormEvent,useState } from "react";import { useRouter } from "next/navigation";import { Loader2,LockKeyhole } from "lucide-react";
export function AdminLogin(){const router=useRouter();const[loading,setLoading]=useState(false);const[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});if(r.ok){router.push("/admin");router.refresh()}else{setError("The email or password is incorrect.");setLoading(false)}}return <form onSubmit={submit} className="glass-card w-full max-w-md"><span className="grid h-12 w-12 place-items-center rounded-xl bg-aqua/10 text-aqua"><LockKeyhole/></span><h1 className="mt-7 font-display text-3xl">Admin sign in</h1><p className="mt-3 text-sm leading-6 text-slate-400">Secure access to enquiries and website operations.</p><label className="mt-8 block text-xs uppercase tracking-widest text-slate-400">Email<input required type="email" name="email" defaultValue="admin@yakafinity.com" className="field mt-2 normal-case tracking-normal"/></label><label className="mt-5 block text-xs uppercase tracking-widest text-slate-400">Password<input required type="password" name="password" className="field mt-2 normal-case tracking-normal"/></label>{error&&<p className="mt-4 text-sm text-red-300">{error}</p>}<button disabled={loading} className="button mt-7 w-full">{loading?<Loader2 className="animate-spin"/>:"Sign in"}</button><p className="mt-5 text-center text-xs text-slate-600">Credentials are configured securely through environment variables.</p></form>}
